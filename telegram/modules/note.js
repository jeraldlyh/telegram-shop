const { Markup } = require("telegraf")
const _ = require("lodash")
const moment = require("moment")
const Constants = require("../constants")
const Template = require("../template")


module.exports = {
    sendConfirmationMessage: async function () {

    }
}